{
    "name": "virtuswap-v1-subgraph",
    "version": "0.1.1",
    "license": "GPL-3.0-or-later",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ graph-test",
        "create-local": "graph create --node http://localhost:8020/ graph-test",
        "remove-local": "graph remove --node http://localhost:8020/ graph-test",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 graph-test",
        "test": "graph test"
    },
    "devDependencies": {
        "@graphprotocol/graph-cli": "0.67.4",
        "@graphprotocol/graph-ts": "0.32.0",
        "matchstick-as": "0.5.0",
        "prettier": "^3.2.5"
    }
}
